<IfModule mod_rewrite.c>
    RewriteEngine On

    # Редирект с HTTP на HTTPS
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Редирект на страницу 404 для URL, содержащих /public/
    RewriteCond %{REQUEST_URI} /public/ [NC]
    RewriteRule ^ - [R=404,L]

    # Перенаправляем все запросы в папку public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# php -- BEGIN cPanel-generated handler, do not edit
# Set the “ea-php74” package as the default “PHP” programming language.
<IfModule mime_module>
    AddHandler application/x-httpd-ea-php74___lsphp .php .php7 .phtml
</IfModule>
# php -- END cPanel-generated handler, do not edit

RewriteCond %{HTTPS} off
RewriteCond %{HTTP:X-Forwarded-SSL} !on
RewriteCond %{HTTP_HOST} ^consult-tm\.by$ [OR]
RewriteCond %{HTTP_HOST} ^www\.consult-tm\.by$
RewriteRule ^/?$ "https://consult-tm.by/" [R=301,L]

RewriteCond %{HTTP_HOST} ^consulttmb\.vh74\.hosterby\.com$ [OR]
RewriteCond %{HTTP_HOST} ^www\.consulttmb\.vh74\.hosterby\.com$
RewriteRule ^/?$ "https://consult-tm.by/" [R=301,L]
